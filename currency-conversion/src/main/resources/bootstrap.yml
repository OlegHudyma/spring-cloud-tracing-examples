spring:
  application:
    name: currency-conversion-service
  zipkin:
    baseUrl: http://localhost:9411/
  sleuth:
    sampler:
      probability: 1.0

server:
  port: 8100

eureka:
  client:
    service-url:
      default-zone: http://localhost:8761/eureka