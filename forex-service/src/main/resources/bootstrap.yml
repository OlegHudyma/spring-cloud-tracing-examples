spring:
  application:
    name: forex-service
  h2:
    console:
      enabled: true
  zipkin:
    baseUrl: http://localhost:9411/
  sleuth:
    sampler:
      probability: 1.0

server:
  port: 8000

eureka:
  client:
    service-url:
      default-zone: http://localhost:8761/eureka

